<schema xmlns="http://purl.oclc.org/dsdl/schematron" >
  <ns prefix="mets" uri="http://www.loc.gov/METS/" />
  <ns prefix="csip" uri="https://DILCIS.eu/XML/METS/CSIPExtensionMETS"/>
  <pattern id="METS_root_element">
      <title>Validate METS root element.</title>
      <rule context="/">
        <assert test="mets:mets">CSIP1000;ERROR;The mets root element is mandatory.</assert>
      </rule>
      <rule context="/mets:mets">
        <assert test="@OBJID_test">CSIP1;ERROR;The mets/@OBJID attribute is mandatory, its value is a string identifier for the METS document. For the package METS document, this should be the name/ID of the package, i.e. the name of the package root folder. For a representation level METS document this value records the name/ID of the representation, i.e. the name of the top-level representation folder.</assert>
        <assert test="(@TYPE_vocabulary_test) and (@TYPE != 'Other' or (@TYPE = 'Other' and @csip:OTHERTYPE != ''))">CSIP2;ERROR;The mets/@TYPE attibute MUST be used to declare the category of the content held in the package, e.g. book, journal, stereograph, video, etc.. Legal values are defined in a fixed vocabulary.</assert>
        <assert test="(@TYPE = 'Other' and @csip:OTHERTYPE) or @TYPE != 'Other'">CSIP3;WARN;When the `mets/@TYPE` attribute has the value "Other" the `mets/@csip:OTHERTYPE` attribute MUST be used to declare the content category of the package/representation. The value can either be "Other" or any other string that are not present in the vocabulary used in the `mets/@TYPE` attribute.</assert>
        <assert test="(@csip:CONTENTINFORMATIONTYPE_vocabulary_test) and (@csip:CONTENTINFORMATIONTYPE != 'OTHER' or (@csip:CONTENTINFORMATIONTYPE = 'OTHER' and @csip:OTHERCONTENTINFORMATIONTYPE != ''))">CSIP4;ERROR;Used to declare the Content Information Type Specification used when creating the package. Legal values are defined in a fixed vocabulary. The attribute is mandatory for representation level METS documents.</assert>
        <assert test="(@csip:CONTENTINFORMATIONTYPE = 'OTHER' and @csip:OTHERCONTENTINFORMATIONTYPE) or @csip:CONTENTINFORMATIONTYPE != 'OTHER'">CSIP5;ERROR;When the mets/@csip:CONTENTINFORMATIONTYPE has the value “OTHER” the mets/@csip:OTHERCONTENTINFORMATIONTYPE must state the content information type.</assert>
        <assert test="@PROFILE">CSIP6;ERROR;The PROFILE attribute MUST contain the URL of the METS profile that the information package conforms with.</assert>
        <assert test="mets:dmdSec">CSIP17;WARN;Must be used if descriptive metadata about the package content is available. NOTE: According to official METS documentation each metadata section must describe one and only one set of metadata. As such, if implementers want to include multiple occurrences of descriptive metadata into the package this must be done by repeating the whole dmdSec element for each individual metadata.</assert>
        <assert test="mets:amdSec">CSIP31;WARN;If administrative / preservation metadata is available, it must be described using the administrative metadata section (amdSec) element. All administrative metadata is present in a single amdSec element.</assert>
        <assert test="mets:fileSec">CSIP58;WARN;The transferred content is placed in the file section in different file group elements, described in other requirements.</assert>
        <assert test="(@csip:CONTENTINFORMATIONTYPE='MIXED' and count(/mets:mets/mets:fileSec/mets:fileGrp/@csip:CONTENTINFORMATIONTYPE) > 0) or not(@csip:CONTENTINFORMATIONTYPE='MIXED')">CSIP62;WARN;When the element "Content Information Type Specification" (CSIP4) has the value "MIXED", then `mets/fileSec/fileGrp` with `CONTENTINFORMATIONTYPE` should exist.</assert>
        <assert test="count(mets:structMap[@LABEL = 'CSIP']) = 1">CSIP80;ERROR;Each METS file must include ONE structural map structMap element used exactly as described here.</assert>
        <assert test="mets:metsHdr">CSIP117;ERROR;There MUST be a general element that describes the package.</assert>
      </rule>
  </pattern>
</schema>
