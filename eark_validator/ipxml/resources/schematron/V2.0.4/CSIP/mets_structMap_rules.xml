<schema xmlns="http://purl.oclc.org/dsdl/schematron" xmlns:xsl="http://www.w3.org/1999/XSL/Transform" queryBinding="xslt2">
  <ns prefix="mets" uri="http://www.loc.gov/METS/" />
  <ns prefix="csip" uri="https://DILCIS.eu/XML/METS/CSIPExtensionMETS"/>
  <ns prefix="xlink" uri="http://www.w3.org/1999/xlink" />
  <pattern id="METS_structmap_element">
    <title>Use of the METS structural map.</title>
    <rule context="/mets:mets/mets:structMap">
      <assert test="@TYPE = 'PHYSICAL'">CSIP81;ERROR;The mets/structMap/@TYPE attribute must take the value “PHYSICAL” from the vocabulary.</assert>
      <assert test="@LABEL = 'CSIP'">CSIP82;ERROR;The mets/structMap/@LABEL attribute value is set to “CSIP” from the vocabulary.</assert>
    </rule>
    <rule context="/mets:mets/mets:structMap[@LABEL = 'CSIP']">
      <assert test="@ID">CSIP83;ERROR;An xml:id identifier for the structural description (structMap) used for internal package references. It must be unique within the package.</assert>
      <assert test="mets:div">CSIP84;ERROR;The structural map MUST comprises a single division.</assert>
      <assert test="mets:div[@LABEL = /mets:mets/@OBJID]">CSIP86;ERROR;The package’s top-level structural division div element’s @LABEL attribute value must be identical to the package identifier, i.e. the same value as the mets/@OBJID attribute.</assert>
    </rule>
    <rule context="/mets:mets/mets:structMap[@LABEL = 'CSIP']/mets:div">
      <assert test="@ID">CSIP85;ERROR;An xml:id identifier must be unique within the package.</assert>
      <assert test="count(mets:div[@LABEL = 'Metadata']) = 1">CSIP88;ERROR;The metadata referenced in the administrative and/or descriptive metadata section is described in the structural map with one sub division.</assert>
      <assert test="count(mets:div[@LABEL = 'Metadata']) = 1">CSIP90;ERROR;The metadata division div element's @LABEL attribute value must be "Metadata".</assert>
      <assert test="count(mets:div[@LABEL = 'Documentation']) &gt;= 1">CSIP93;WARN;There SHOULD be a div-element with the @LABEL='Documentation' nested in the div-element with the @LABEL='CSIP'.</assert>
      <assert test="count(mets:div[@LABEL = 'Documentation']) &lt;= 1">CSIP93;ERROR;There MUST be maximum one div-element with the @LABEL='Documentation' nested in the div-element with the @LABEL='CSIP'.</assert>
      <assert test="count(mets:div[@LABEL = 'Documentation']) &lt;= 1">CSIP95;ERROR;The documentation division div element in the package uses the value "Documentation" from the vocabulary as the value for the @LABEL attribute.</assert>
      <assert test="count(mets:div[@LABEL = 'Schemas']) &gt;= 1">CSIP97;WARN;There SHOULD be a div-element with the @LABEL='Schemas' nested in the div-element with the @LABEL='CSIP'.</assert>
      <assert test="count(mets:div[@LABEL = 'Schemas']) &lt;= 1">CSIP97;ERROR;There MUST be maximum one div-element with the @LABEL='Schemas' nested in the div-element with the @LABEL='CSIP'.</assert>
      <assert test="count(mets:div[@LABEL = 'Schemas']) &lt;= 1">CSIP99;ERROR;The schema division div element in the package uses the value "Schemas" from the vocabulary as the value for the @LABEL attribute.</assert>
      <assert test="mets:div[@LABEL = 'Representations']">CSIP101;WARN;When no representations are present the content referenced in the file section file group with @USE attribute value “Representations” is described in the structural map as a single sub division.</assert>
      <assert test="mets:div[@LABEL = 'Representations']">CSIP103;ERROR;The package's content division div element must have the @LABEL attribute value "Representations", taken from the vocabulary.</assert>
      <assert test="mets:div[@LABEL = 'Representations']/mets:div">CSIP105;WARN;When a package consists of multiple representations, each described by a representation level METS.xml document, there should be a discrete representation div element for each representation.</assert>
    </rule>
    <rule context="/mets:mets/mets:structMap[@LABEL = 'CSIP']/mets:div/mets:div[@LABEL = 'Metadata']">
      <assert test="@ID">CSIP89;ERROR;An xml:id identifier must be unique within the package.</assert>
      <assert test="every $admid_part in tokenize(@ADMID, '\s+') satisfies some $id in /mets:mets/mets:amdSec/*[@STATUS = 'CURRENT']/@ID satisfies $admid_part = $id">CSIP91;ERROR;When there is administrative metadata and the amdSec is present, all administrative metadata MUST be referenced via the administrative sections different identifiers.</assert>
      <assert test="every $dmdid_part in tokenize(@DMDID, '\s+') satisfies some $id in /mets:mets/mets:dmdSec/*[@STATUS = 'CURRENT']/@ID satisfies $dmdid_part = $id">CSIP92;ERROR;When there are descriptive metadata and one or more dmdSec is present, all descriptive metadata MUST be referenced via the descriptive section identifiers.</assert>
    </rule>
    <rule context="/mets:mets/mets:structMap[@LABEL = 'CSIP']/mets:div/mets:div[@LABEL = 'Documentation']">
      <assert test="@ID">CSIP94;ERROR;An xml:id identifier must be unique within the package.</assert>
      <assert test="count(mets:fptr) = count(/mets:mets/mets:fileSec/mets:fileGrp[@USE = 'Documentation'])">CSIP96;ERROR;All file groups containing documentation described in the package are referenced via the relevant file group identifiers. There MUST be one file group reference per fptr element.</assert>
    </rule>
    <rule context="/mets:mets/mets:structMap[@LABEL = 'CSIP']/mets:div/mets:div[@LABEL = 'Documentation']/mets:fptr">
      <assert test="@FILEID and some $fileGrp in /mets:mets/mets:fileSec/mets:fileGrp[@USE = 'Documentation'] satisfies $fileGrp/@ID = @FILEID">CSIP116;ERROR;A reference, by ID, to the “Documentation” file group.</assert>
    </rule>
    <rule context="/mets:mets/mets:structMap[@LABEL = 'CSIP']/mets:div/mets:div[@LABEL = 'Schemas']">
      <assert test="@ID">CSIP98;ERROR;An xml:id identifier must be unique within the package.</assert>
      <assert test="count(mets:fptr) = count(/mets:mets/mets:fileSec/mets:fileGrp[@USE = 'Schemas'])">CSIP100;ERROR;All file groups containing schemas described in the package are referenced via the relevant file group identifiers. There MUST be one file group reference per fptr element.</assert>
    </rule>
    <rule context="/mets:mets/mets:structMap[@LABEL = 'CSIP']/mets:div/mets:div[@LABEL = 'Schemas']/mets:fptr">
      <assert test="@FILEID and some $fileGrp in /mets:mets/mets:fileSec/mets:fileGrp[@USE = 'Schemas'] satisfies $fileGrp/@ID = @FILEID">CSIP118;ERROR;The pointer to the identifier for the “Schema” file group.</assert>
    </rule>
    <rule context="/mets:mets/mets:structMap[@LABEL = 'CSIP']/mets:div/mets:div[@LABEL = 'Representations']">
      <assert test="@ID">CSIP102;ERROR;An xml:id identifier must be unique within the package.</assert>
      <assert test="mets:fptr">CSIP104;ERROR;All file groups containing documentation described in the package are referenced via the relevant file group identifiers. There MUST be one file group reference per fptr element.</assert>
    </rule>
    <rule context="/mets:mets/mets:structMap[@LABEL = 'CSIP']/mets:div/mets:div[@LABEL = 'Representations']/mets:fptr">
      <assert test="@FILEID">CSIP119;ERROR;The pointer to the identifier for the Representations file group.</assert>
    </rule>
    <rule context="/mets:mets/mets:structMap[@LABEL = 'CSIP']/mets:div/mets:div[@LABEL = 'Representations']/mets:div">
      <assert test="@ID">CSIP106;ERROR;Mandatory, xml:id identifier must be unique within the package.</assert>
      <assert test="@LABEL">CSIP107;ERROR;The package’s representation division div element @LABEL attribute value must be the path to the representation level METS document.</assert>
      <assert test="mets:mptr">CSIP109;ERROR;The division div of the specific representation includes one occurrence of the METS pointer mptr element, pointing to the appropriate representation METS file.</assert>
    </rule>
    <rule context="/mets:mets/mets:structMap[@LABEL = 'CSIP']/mets:div/mets:div[@LABEL = 'Representations']/mets:mptr">
      <assert test="@xlink:title">CSIP108;ERROR;The file group containing the files described in the package are referenced via the relevant file group identifier.</assert>
      <assert test="@xlink:href">CSIP110;ERROR;MUST point to the actual location of the resource. We recommend recording a URL type filepath within this attribute.</assert>
      <assert test="@xlink:type = 'simple'">CSIP111;ERROR;Attribute used with the value “simple”. Value list is maintained by the xlink standard.</assert>
      <assert test="@LOCTYPE = 'URL'">CSIP112;ERROR;The locator type is always used with the value “URL” from the vocabulary in the attribute.</assert>
    </rule>
  </pattern>
</schema>
